---
import '@fontsource-variable/onest';
import '@fontsource-variable/jetbrains-mono';
import '../styles/global.css';
import Navbar from '../components/Navbar.astro';

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="PMC250 - Comunidad de Simulación Arma 3" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<title>{title} | PMC250</title>
	</head>
	<body class="bg-pmc-950 text-white font-sans antialiased overflow-x-hidden selection:bg-pmc-accent selection:text-black">
		
		<div class="fixed inset-0 z-[-1] pointer-events-none">
            <div class="absolute inset-0 bg-[linear-gradient(to_right,#ffffff03_1px,transparent_1px),linear-gradient(to_bottom,#ffffff03_1px,transparent_1px)] bg-size-[40px_40px]"></div>
            <div class="absolute inset-0 bg-radial-[circle_at_center] from-transparent to-pmc-950"></div>
		</div>

        <Navbar />

		<main class="relative z-10 flex flex-col min-h-screen">
			<slot />
		</main>

	</body>
</html>